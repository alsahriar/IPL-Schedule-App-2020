<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- TODO Remove the following "exclude" elements to make them a part of the auto backup -->
    <!-- Exclude the shared preferences file that contains the GCM registrationId -->
    <exclude
        domain="sharedpref"
        path="team_item_count.xml" />
    <exclude
        domain="sharedpref"
        path="prefs.xml" />
    <exclude
        domain="sharedpref"
        path="home_item_count.xml" />
    <exclude
        domain="sharedpref"
        path="TapTG.xml" />
</full-backup-content>
